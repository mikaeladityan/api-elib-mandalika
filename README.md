### Wajib melakukan

```json
    npm run generate:app-code
```

<small>Output: `59a9b699de23efde7c1068347dbe7dc4`</small>

Selanjutnya `copy` hasil generate dan `paste` di _`.env`_ dengan _`key`_ `company:generateCode`

```
npm install
npm run dev
```

```
open http://localhost:3000
```
